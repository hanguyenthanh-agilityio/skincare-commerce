---
// Types
import type { TBlog } from '@/types';

// Components
import { BlogList } from '@/components';
import { SectionWrapper } from '@/layouts';

interface Props {
  title: string;
  subtitle: string;
  description?: string;
  ctaHref?: string;
  blogs: TBlog[];
  className?: string;
}

const { title, subtitle, description, ctaHref, blogs, className } = Astro.props;
---

<SectionWrapper {title} {subtitle} {description} {ctaHref} {className}>
  <BlogList blogs={blogs} />
</SectionWrapper>
