---
// Types
import type { Locale, TProduct } from '@/types';

// Components
import { ProductList } from '@/components';
import { SectionWrapper } from '@/layouts';

interface Props {
  title: string;
  subtitle: string;
  description?: string;
  ctaLabel?: string;
  ctaHref?: string;
  products: TProduct[];
  limitDesktop?: number;
  limitMobile?: number;
  locale: Locale;
  className?: string;
}

const {
  title,
  subtitle,
  description,
  ctaHref,
  ctaLabel,
  products,
  limitDesktop,
  limitMobile,
  locale,
  className,
} = Astro.props;
---

<SectionWrapper {title} {subtitle} {description} {ctaHref} {ctaLabel} {className}>
  <ProductList {locale} {products} {limitMobile} {limitDesktop} />
</SectionWrapper>
