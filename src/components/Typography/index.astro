---
import { cn } from '@/lib/utils';

interface Props {
  title: string;
  level: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6' | 'p' | 'span';
  className?: string;
}

const { level, title, className } = Astro.props;

const Tag = level;

const defaultClasses: Record<string, string> = {
  h1: 'text-4xl/10',
  h2: 'text-3xl/10',
  h3: 'text-xl/8 font-medium',
  h4: 'text-lg',
  h5: 'text-base',
  h6: 'text-sm',
  p: 'text-base/7',
  span: 'text-sm/6',
};
---

<Tag class={cn('text-destructive-foreground', defaultClasses[level], className)}>
  {title}
</Tag>
