---
import RemoteImage from '@/components/RemoteImage/index.astro';

export interface Props {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props;
---

<div class="item">
  <RemoteImage src={src} alt={alt} aspectRatio="1/1" />
</div>
