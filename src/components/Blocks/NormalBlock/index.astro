---
import { cn } from '@/lib/utils';

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<p class={cn('text-sm/6 tracking-[-1%] text-destructive-foreground', className)}>
  <slot />
</p>
