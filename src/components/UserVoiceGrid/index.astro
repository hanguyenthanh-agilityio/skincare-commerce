---
import { UserVoiceItem } from '@/components';
import { userVoiceImages } from '@/data/userVoiceImages';
---

<div class="flex justify-center">
  <div class="grid grid-cols-3 gap-4 mt-10">
    {userVoiceImages.map((item) => <UserVoiceItem {...item} />)}
  </div>
</div>
