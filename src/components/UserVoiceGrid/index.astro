---
// Components
import { UserVoiceItem } from '@/components';

// Constants
import { USER_VOICE_DATA } from '@/constants';
---

<div class="flex justify-center">
  <div class="grid grid-cols-3 gap-2 md:gap-3 mt-10">
    {USER_VOICE_DATA.map((item) => <UserVoiceItem {...item} />)}
  </div>
</div>
